Not fully correct yet: Task 2’s core sanitization is solid, but the new API suite fails, so it should not be marked complete.

- Blocking: both `redis-down` tests fail. CI sets `AUTH_DISABLE_RATE_LIMITS=true`, causing [`rate-limit.ts`](/C:/Users/PC-store/Documents/Projets/Nodiox/apps/auth/src/lib/rate-limit.ts:151) to return before reaching the test-only Redis simulation at line 161. Result: expected 429s become 401/500. Move the gated simulation before the bypass, or change the test environment design.

- The walkthrough overstates validation: it says Sentry extras are tested, but [`redaction.test.ts`](/C:/Users/PC-store/Documents/Projets/Nodiox/apps/auth/src/__tests__/unit/redaction.test.ts:157) captures only console output. The implementation does sanitize Sentry extras in [`security-log.ts`](/C:/Users/PC-store/Documents/Projets/Nodiox/apps/auth/src/lib/security-log.ts:43), but that behavior needs a mocked-Sentry assertion.

- The simulation gate relies only on `AUTH_TEST_SIMULATION=1`; it is not additionally prevented in non-test deployments. An accidental production/staging variable could let callers trigger fixed simulated failures via a header. This is a defense-in-depth issue, not a data leak.

What is correct:
- All reviewed auth mutation routes now return stable public errors rather than provider exception text.
- Both logging paths pass through centralized redaction before console/Sentry.
- `test:unit` passes: 9/9.
- `type-check` passes.
- `test:api` fails: 13/15, solely the two Redis simulation tests.
- `lint` still fails because `eslint` is not directly resolvable in the auth workspace.

No files were changed.